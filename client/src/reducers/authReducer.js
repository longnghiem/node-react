import { FETCH_USER } from '../actions/types';

const reducer = (state = {}, action) => {
  console.log(action);
  switch (action.type) {
    default:
      return state;
  }
};

export default reducer;
